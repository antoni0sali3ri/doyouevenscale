<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- "Mark Favorite", should appear as action button if possible -->
    <item
        android:id="@+id/actionManageData"
        android:icon="@drawable/ic_edit"
        android:title="@string/action_manage_data_title"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/actionFullScreen"
        android:icon="@drawable/ic_full_screen"
        android:title="@string/action_full_screen_title"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/actionSettings"
        android:title="@string/action_settings_title"
        app:showAsAction="never" />
</menu>